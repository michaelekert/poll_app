<div class="min-h-screen flex items-center justify-center bg-gray-100 p-4">
  <div class="w-full max-w-xl bg-white rounded-2xl shadow-lg p-6 space-y-6">
    <h1 class="text-2xl font-bold text-center">Dostępne ankiety</h1>

    <ul class="space-y-4">
      <% @polls.each do |poll| %>
        <li class="border rounded-lg hover:bg-gray-50 transition p-4 flex justify-between items-center">
          <span class="text-lg"><%= poll.question %></span>
          <%= link_to "Zobacz", poll_path(poll), class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
        </li>
      <% end %>
    </ul>
    <h1>Oddano: <%= @votes %></h1>
  </div>
</div>